apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: sid-devboard
spec:
  selector:
    app: devboard-backend
  ports:
    - protocol: TCP
      port: 8080 #this service to be connected
      targetPort: 8080 #pod port
    
