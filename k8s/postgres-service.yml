apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: sid-devboard
spec:
  clusterIP: None
  selector:
    app: devboard-postgres
  ports:
    - protocol: TCP
      port: 5432 #this service to be connected
      targetPort: 5432 #pod port
    
